describe('auth', () => {
  describe('login', () => {
    it('must be able to login using the correct username and password', () => {
      // noop
    });

    it('must not be able to login with an incorrect password', () => {
      // noop
    });
  });

  describe('cli', () => {
    it('must be able to login via the cli', () => {
      // noop
    });
  });
});
