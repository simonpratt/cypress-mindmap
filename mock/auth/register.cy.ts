describe('auth', () => {
  describe('register', () => {
    it('must be able to register using a valid email and password', () => {
      // noop
    });

    it('must not be able to register with an invalid email', () => {
      // noop
    });
  });
});
